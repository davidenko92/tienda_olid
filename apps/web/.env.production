# Production - VPS with custom domain
# Nginx reverse proxy - las peticiones /api/* se redirigen al backend en :3000
VITE_API_URL=https://olid.store/api
# Images are served directly from /images/ (not /api/images/)
VITE_IMAGES_URL=https://olid.store
